{
  "name": "ims-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Permanent Playwright E2E regression suite for IMS 2.0. Runs against a CI-seeded LOCAL stack — never prod.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0"
  }
}
