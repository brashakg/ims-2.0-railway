# IMS 2.0 Backend Dependencies
# ===============================

# Web Framework
fastapi==0.136.3
uvicorn[standard]==0.48.0

# Data Validation
pydantic==2.13.4
pydantic-settings==2.14.1

# Authentication & Security
pyjwt==2.12.1
bcrypt==4.2.0
python-multipart==0.0.29
# RSA-SHA256 signing for QZ Tray silent label printing (labels.py). The
# import is lazy + fail-soft, so a missing wheel degrades to HTML printing.
# 44.0.1 is a security patch (no API change) over 44.0.0: it ships an OpenSSL
# fix for CVE-2024-12797 (the wheel statically links OpenSSL). The two later
# CVEs in this package (PYSEC-2026-35 X.509 DNS name-constraint bypass,
# CVE-2026-26007 EC public-key subgroup check) require a major bump to 46.x;
# neither code path is exercised here (we only load our OWN PEM private key
# and RSA-sign), so they are deferred to avoid a breaking upgrade.
cryptography==44.0.1

# Database
pymongo==4.17.0
# Read-only bridge to the e-commerce (BVI) Postgres for online catalog/stock
psycopg2-binary==2.9.10

# HTTP Client (for JARVIS AI)
httpx==0.28.1

# Agent Scheduler
apscheduler==3.11.2

# Caching
redis==5.3.1

# Utilities
email-validator==2.3.0
python-dateutil==2.9.0.post0

# Production Server (optional, uvicorn is primary)
gunicorn==23.0.0
