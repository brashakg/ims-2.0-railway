# IMS 2.0 Backend Dependencies
# ===============================

# Web Framework
fastapi==0.136.1
uvicorn[standard]==0.46.0

# Data Validation
pydantic==2.13.4
pydantic-settings==2.14.1

# Authentication & Security
pyjwt==2.12.1
bcrypt==4.2.0
python-multipart==0.0.29
# RSA-SHA256 signing for QZ Tray silent label printing (labels.py). The
# import is lazy + fail-soft, so a missing wheel degrades to HTML printing.
# 48.0.0 fixes PYSEC-2026-35 (X.509 DNS name-constraint bypass) and
# CVE-2026-26007 (EC public-key subgroup check). API is backwards-compatible
# for our RSA-only usage (load own PEM key + sign). Upgraded 2026-06-02.
cryptography==48.0.0

# Database
pymongo==4.17.0
# Read-only bridge to the e-commerce (BVI) Postgres for online catalog/stock
psycopg2-binary==2.9.10

# HTTP Client (for JARVIS AI)
httpx==0.28.1

# Agent Scheduler
apscheduler==3.11.2

# Caching
redis==5.3.1

# Utilities
email-validator==2.3.0
python-dateutil==2.9.0.post0

# Production Server (optional, uvicorn is primary)
gunicorn==23.0.0
