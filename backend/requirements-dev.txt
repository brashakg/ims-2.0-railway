# IMS 2.0 Backend - Development Dependencies
# ==========================================

# Testing
pytest==7.4.4
pytest-asyncio==0.23.2
pytest-cov==4.1.0
pytest-mock==3.12.0

# Code Quality
black==24.1.1
pylint==3.0.3
# Teaches pylint about Pydantic v2 dynamic methods (model_dump, field_validator, etc.)
# so BaseModel subclasses aren't flagged as having no such members.
pylint-pydantic==0.3.2
mypy==1.8.0
types-python-dateutil==2.9.0.20240316

# Security
bandit==1.7.6
safety==3.0.1
# safety 3.0.1 is incompatible with modern click/typer internals; the CI step
# is marked continue-on-error so we don't chase its dep hell. Bandit still blocks.

