════════════════════════════════════════════════════════════════════════════════
AFTER RAILWAY MCP SETUP IS COMPLETE, COPY THIS ENTIRE MESSAGE AND PASTE IT
INTO CLAUDE CODE
════════════════════════════════════════════════════════════════════════════════

@railway-mcp-server I need you to diagnose why my IMS 2.0 backend is returning
HTTP 503 errors on all API endpoints except /auth/login.

Please do the following in order:

1. **Check if everything is running:**
   - Use check-railway-status to verify you're logged in
   - Use list-services to show me all running services

2. **Get the deployment logs:**
   - Use get-logs to retrieve the last 500 lines of backend logs
   - Look for any error messages, crashes, or connection failures
   - Show me the full logs

3. **Check environment variables:**
   - Use list-variables to show all 29 environment variables
   - Specifically check: API_HOST, API_PORT, MONGO_HOST, MONGO_DATABASE,
     MONGO_USERNAME, MONGO_PASSWORD, JWT_SECRET_KEY

4. **Report findings:**
   - Tell me if the service is running (green/online)
   - Tell me what errors appear in the logs
   - Tell me if all required environment variables are set
   - Based on the logs, identify the root cause of the 503 errors

5. **After I see the results:**
   - If it's a MongoDB connection issue → I'll provide a fix
   - If it's a configuration issue → I'll update the variables
   - If it's an API code issue → I'll deploy the fixed code

Please start by checking the status and retrieving the logs.

════════════════════════════════════════════════════════════════════════════════
