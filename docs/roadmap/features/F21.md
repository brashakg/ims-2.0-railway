> ## [WARNING] BINDING CORRECTIONS -- READ `../CORRECTIONS.md` FIRST
> BINDING (CORRECTIONS P0-6): depends on the E3-shim (see ENGINES.md). `stock_units.status` is a FREE STRING (no enum change). You MUST exclude QUARANTINED from EVERY on-hand rollup: product_repository.find_available + inventory.py ~107/2147/2274/2366/2731. POS safety = find_available filtering AVAILABLE. Intent test: quarantine the only AVAILABLE unit -> POS sell returns 409.
> _Precedence: DECISIONS > CORRECTIONS > this packet. If this packet conflicts with CORRECTIONS, CORRECTIONS wins._

---

I now have all the grounding I need. Let me write the build packet.

# Packet F21: #21 Defective quarantine barcoding

## Current behavior (what the code does today — file:line)

`stock_units` has a `status` field but its live enum values are `AVAILABLE`, `RESERVED`, `TRANSFERRED`, `SOLD`, and `DAMAGED` (with `DAMAGED` used inconsistently). Specifically:

- `inventory.py:88–91` — `generate_barcode()` mints `"{store_id[:3]}-{UUID[:8]}"` per unit; the unit is written with `"status": "AVAILABLE"` (`inventory.py:820`, `1024`).
- `inventory.py:116` — on-hand query treats `["AVAILABLE", "IN_STOCK"]` as sellable; DAMAGED units have no special path and remain in the stock collection without any label or quarantine lifecycle.
- `returns.py:70` — `ItemCondition = Literal["GOOD", "OPENED", "DAMAGED"]`; `returns.py:768–910` (`_restock_good_items`) silently skips DAMAGED units (they stay as-is in the stock collection with no quarantine flag, no label, no audit).
- `transfers.py:300–380` (`_apply_ship_stock_move`) — claims only `status=AVAILABLE` units; a DAMAGED unit is quietly skipped but not blocked with an error, so a manager could manually transfer it if the guard is bypassed.
- `labels.py` — the QZ Tray signing infrastructure (`/print/qz/cert`, `/print/qz/sign`) and product-label payload (`/workshop/product-label`) exist and work. There is **no quarantine label endpoint**.
- `vendor_returns.py:39–45` (`VendorReturnCreate`) — schema has no `stock_ids` field; there is no physical-unit linkage between a vendor return and the defective units being shipped back.
- `inventory.py:3278` — a separate `_SERIAL_STATUSES` set contains `{"IN_STOCK","SOLD","WARRANTY_CLAIM","DAMAGED","LOST_STOLEN"}` used for serial-tracked items only, confirming `QUARANTINED` does not exist anywhere in the codebase.
- `finance.py:459` — `check_period_locked(db, posting_date)` exists and is used by `orders.py:1164`. It is **not** called anywhere in inventory status-change paths today.

**Net**: a defective unit returned from a customer or received damaged on GRN has no dedicated status, no physical red label, no manager-only gate, and no link to a vendor RTV. A cashier can re-shelve it accidentally because there is nothing in the system to stop them.

## Intended behavior (full intent)

When a store manager marks a physical unit defective, the system must:

1. Atomically flip the unit's status to the free-string value `QUARANTINED` (NOT an enum / `$jsonSchema` change — `stock_units.status` is a free string; not overloading `DAMAGED`) and record who did it, why, and when.
2. Immediately gate that unit out of every sellable path: POS catalog (`status=AVAILABLE` filter already excludes it), transfers, and blind-count sellable-on-hand all see zero for that unit.
3. Force the manager to print a bright-red "QUARANTINED — DO NOT SHELVE" sticker (QZ Tray or HTML fallback) carrying the stock barcode, reason, date, and store name before the unit can be cleared from the quarantine queue. Until printed, the queue shows an unlabeled-unit warning.
4. Provide a Quarantine Queue tab showing all quarantined units for the store with one-click "Create RTV" pre-filling the vendor returns form.
5. Let a manager explicitly lift the quarantine (back to `AVAILABLE` for mis-quarantine correction) with a mandatory audit reason, via a separate `lift-quarantine` endpoint — not by simply re-patching status.
6. Emit a `stock.quarantined` event via the existing event bus so TASKMASTER can raise a follow-up task if the unit stays unlinked to an RTV after 7 days (the DECISIONS.md recommended default).
7. Write every status transition (`AVAILABLE→QUARANTINED`, `QUARANTINE→AVAILABLE`, `QUARANTINE→SCRAPPED`) to `audit_logs` via `AuditRepository.create` (the hash-chained façade per ENGINES.md §"Audit append" locked contract). Never call `append_audit_entry` directly.
8. Luxury brands (Cartier, Chopard, Bvlgari, Gucci, Prada, Versace, Burberry — the same list in `pricing_caps.py`) get an extra "BRAND AUTH REQUIRED" line on the label (per the F21 owner decision option b, which is the more rigorous choice for luxury optical).

No Tally entry at quarantine time (per F21 owner decision: only when RTV is formally booked).

## Delta to build (the precise work)

**Backend — inventory.py:**
- Write the free-string value `QUARANTINED` to `stock_units.status` (NO enum / `$jsonSchema` change; distinct from `DAMAGED`). Per CORRECTIONS P0-6 the status is a free string — the E3-shim adds no enum.
- Add `PATCH /api/v1/inventory/stock/{stock_id}/quarantine` endpoint (roles: `_STOCK_MANAGER_ROLES` = `ADMIN, AREA_MANAGER, STORE_MANAGER`).
- Add `PATCH /api/v1/inventory/stock/{stock_id}/lift-quarantine` endpoint (same roles; mandatory `lift_reason`).
- Add `GET /api/v1/inventory/stock/quarantined` endpoint (store-scoped; query params: `store_id`, `rtv_vendor_id`, `label_printed: bool`, `date_from`, `date_to`).
- Guard `_apply_ship_stock_move` in `transfers.py:~330` to explicitly reject `QUARANTINED` units with a 400 (not just skip them silently).

**Backend — labels.py:**
- Add `POST /api/v1/labels/quarantine/{stock_id}` endpoint — builds the QZ Tray label payload (red background, "QUARANTINED — DO NOT SHELVE", Code128 barcode, reason, date, store, luxury brand line when applicable), flips `quarantine_label_printed=true`, writes audit row.

**Backend — vendor_returns.py:**
- Add optional `stock_ids: list[str]` field to `VendorReturnCreate` schema.
- When `stock_ids` are supplied, backfill `rtv_vendor_id` on those `stock_units` docs and write an audit row linking them.

**Frontend:**
- `InventoryPage.tsx`: Add "Mark Quarantine" action button per unit row (STORE_MANAGER+ only, hidden for SOLD/TRANSFERRED). Modal with reason dropdown + optional vendor selector + notes. On confirm: PATCH quarantine then POST label (print via QZ Tray/browser). Add red `QUARANTINED` status chip.
- New "Quarantine Queue" tab on `InventoryPage.tsx`: lists all QUARANTINED units for the store, red badge showing unlabeled count, "Print" CTA per unlabeled unit, "Create RTV" CTA pre-filling vendor returns.
- `SerialNumberTracker.tsx` (if present): add "Mark Quarantine" in serial detail modal for high-value items.

**No feature flag needed** — touches no POS payment path, no pricing logic, no order creation.

## Data model (collections/fields; new vs existing; migration)

**Collection: `stock_units` (EXISTING — additive fields only, no migration)**

| Field | Type | New/Existing | Notes |
|---|---|---|---|
| `status` | free string | EXISTING (no schema change) | Write the free-string value `QUARANTINED` (NO enum / `$jsonSchema` edit). No existing query breaks — POS/transfer/on-hand filters already use explicit allowlists (`AVAILABLE`, `RESERVED`), so QUARANTINED is excluded simply by not being in any allowlist. |
| `quarantine_reason` | string | NEW, optional | Enum: `DEFECTIVE`, `SCRATCHED`, `CUSTOMER_RETURN_DAMAGED`, `QC_FAILED_WORKSHOP`, `RECEIVED_DAMAGED`, `OTHER`. |
| `quarantine_at` | datetime | NEW, optional | IST-stamped on quarantine write. |
| `quarantine_by` | string (user_id) | NEW, optional | From JWT actor. |
| `quarantine_by_name` | string | NEW, optional | Denormalized for display (avoids a join on the queue read). |
| `quarantine_notes` | string | NEW, optional | Free-text, max 200 chars. |
| `quarantine_label_printed` | bool | NEW, optional | Default `false`; flipped to `true` by label endpoint. |
| `rtv_vendor_id` | string | NEW, optional | Set at quarantine time (if RTV vendor is known) or backfilled when RTV is created. |

**Collection: `audit_logs` (EXISTING — new rows only)**

Every status transition writes one row via `get_audit_repository().create(...)`. Shape follows the existing pattern used in `labels.py:321–334`:
```python
{
  "action": "STOCK_QUARANTINED",          # or "QUARANTINE_LIFTED", "QUARANTINE_LABEL_PRINTED"
  "entity_type": "STOCK_UNIT",
  "entity_id": stock_id,
  "store_id": store_id,
  "user_id": actor_user_id,
  "before_state": {"status": "AVAILABLE"},
  "after_state": {"status": "QUARANTINED", "quarantine_reason": reason},
  "detail": {...}
}
```

**Collection: `vendor_returns` (EXISTING — additive field on create)**

`VendorReturnCreate` schema gains `stock_ids: Optional[List[str]] = None`. No migration needed; existing return docs without this field continue to work.

No new collection. No index changes required (quarantined unit queries use the existing `store_id` + `status` compound; if the query is slow, a sparse index `{store_id:1, status:1}` can be added — but this index likely already exists given on-hand rollup queries use it).

## Backend (endpoints + services + which ENGINE calls)

**`PATCH /api/v1/inventory/stock/{stock_id}/quarantine`** — extend `inventory.py`

- Roles: `_STOCK_MANAGER_ROLES` (`ADMIN, AREA_MANAGER, STORE_MANAGER`; `SUPERADMIN` auto-passes via `require_roles`).
- Guard: unit must be `AVAILABLE` or `DAMAGED` (not `SOLD`, `TRANSFERRED`, `QUARANTINED`). Return 409 with machine code `"already_quarantined"` or `"not_eligible"`.
- Period-lock check: call `from .finance import check_period_locked` (same import used by `orders.py:1164`) against the current date. Fail-soft per `finance.py:462` contract.
- Store-scope guard: reuse `can_access_store_scoped` (imported in `labels.py:51` from `dependencies`).
- Write: `stock_repo.update(stock_id, {"status": "QUARANTINED", "quarantine_reason": ..., "quarantine_at": now_ist, "quarantine_by": user_id, "quarantine_by_name": ..., "quarantine_notes": ..., "quarantine_label_printed": False, "rtv_vendor_id": ...})`.
- Audit: `get_audit_repository().create({"action": "STOCK_QUARANTINED", "entity_type": "STOCK_UNIT", "entity_id": stock_id, ...})`. — **calls `AuditRepository.create`, never `append_audit_entry` directly** (ENGINES.md locked).
- Event bus: `registry.dispatch_event("stock.quarantined", {"stock_id": ..., "store_id": ..., "reason": ..., "actor_id": ...})` — fail-soft; lets TASKMASTER raise an RTV follow-up task after 7 days.
- Return: updated `stock_unit` doc.

**`PATCH /api/v1/inventory/stock/{stock_id}/lift-quarantine`** — extend `inventory.py`

- Roles: same `_STOCK_MANAGER_ROLES`.
- Body: `{lift_reason: str}` (mandatory, min 5 chars).
- Guard: unit must be `QUARANTINED`. Return 409 `"not_quarantined"` if not.
- Write: `stock_repo.update(stock_id, {"status": "AVAILABLE", "quarantine_lifted_at": now_ist, "quarantine_lifted_by": user_id, "quarantine_lift_reason": lift_reason, "quarantine_label_printed": False})`.
- Audit: `AuditRepository.create({"action": "QUARANTINE_LIFTED", ...})`.
- **No E4 (approval/PIN) gate**: store manager self-approval is sufficient (F21 owner decision option a — "store manager can lift quarantine unilaterally, audit logged"). E4 is not yet in the Wave 3 build path anyway.

**`GET /api/v1/inventory/stock/quarantined`** — extend `inventory.py`

- Roles: `_STOCK_MANAGER_ROLES` plus read-only for ACCOUNTANT (read-only access per RBAC table in F21 spec).
- Store-scope: non-HQ sees their store only; AREA_MANAGER/ADMIN/SUPERADMIN can pass `store_id` param.
- Mongo aggregation: `{$match: {status: "QUARANTINED", store_id: ...}}` + lookup on `products` for name/brand/category.
- Returns list with `quarantine_label_printed` flag, count of unlabeled units in summary.

**`POST /api/v1/labels/quarantine/{stock_id}`** — extend `labels.py`

- Roles: `_STOCK_MANAGER_ROLES`.
- Fetches stock_unit (fail: 404) + product name/brand/category via `get_product_repository()` (same pattern as `get_product_label` at `labels.py:505`).
- Guard: unit must be `QUARANTINED`. Return 400 `"not_quarantined"` otherwise.
- Store-scope guard: `can_access_store_scoped`.
- Builds label payload following the same structure as the existing `get_product_label` response plus:
  - `label_type: "QUARANTINED"`
  - `header: "QUARANTINED -- DO NOT SHELVE"` (ASCII double-dash, no unicode em-dash)
  - `sub_header: "RTV Pending"` (or `"No RTV linked"` if `rtv_vendor_id` is absent)
  - `background_color: "#DC2626"` (Tailwind red-600, consistent with existing red-600 usage)
  - `quarantine_reason`, `quarantine_at`, `quarantine_by_name`, `store_name`
  - `barcode_value`: the existing `stock_unit.barcode` (already a `{store[:3]}-{UUID[:8]}` string from `generate_barcode`)
  - Luxury brand check: if `product.brand` is in `["Cartier","Chopard","Bvlgari","Gucci","Prada","Versace","Burberry"]` (same list as `pricing_caps.py` luxury caps), add `"luxury_brand_line": "BRAND AUTHORIZATION REQUIRED"`.
- Flips `quarantine_label_printed=true` on the stock_unit via `stock_repo.update(...)`.
- Audit: `get_audit_repository().create({"action": "QUARANTINE_LABEL_PRINTED", ...})`.
- Returns the label payload (same shape as `get_product_label`; frontend sends it to QZ Tray or opens the browser print dialog).

**`vendor_returns.py` — extend `VendorReturnCreate`**

Add `stock_ids: Optional[List[str]] = None` to the Pydantic schema. In the create endpoint, after the return doc is inserted, if `stock_ids` is provided, run:
```python
for sid in stock_ids:
    db.stock_units.update_one(
        {"stock_id": sid, "status": "QUARANTINED"},
        {"$set": {"rtv_vendor_id": vendor_return_id}}
    )
    get_audit_repository().create({"action": "QUARANTINE_LINKED_RTV", "entity_type": "STOCK_UNIT", "entity_id": sid, ...})
```

**`transfers.py` — `_apply_ship_stock_move` hardening**

At `transfers.py:~330`, the candidate query currently filters `status=AVAILABLE`. Add an explicit check: after fetching candidates, if any candidate has `status=QUARANTINED`, log a warning and exclude it. Additionally add a pre-flight check on the line level: if the caller explicitly passes `stock_ids` on a transfer line, verify none are `QUARANTINED` and return a 400 before shipping.

**Engine calls (per ENGINES.md "call engines, never reimplement"):**

- `AuditRepository.create` — the locked audit façade (hash-chains internally via `append_audit_entry`). Called from both `inventory.py` and `labels.py` quarantine endpoints. **Never call `append_audit_entry` directly.**
- `registry.dispatch_event("stock.quarantined", ...)` — the existing event bus (`backend/agents/registry.py`). Fail-soft per the existing contract.
- E3 (`item_events.record_event`) is a Wave-3 engine not yet built. F21 does NOT wait for E3. Instead, use the existing `stock_repo.update(...)` + `AuditRepository.create(...)` pattern (identical to how `scan_advance` works in `labels.py:300–334`). When E3 lands, its `record_event` call will wrap these two writes — this is the documented "behavior-preserving rewire" path in E3.md.
- E4 (approval/PIN) is not required for this feature (no financial impact, store manager self-approval is the locked answer for quarantine lift).
- `check_period_locked` from `finance.py:459` — reuse for the quarantine PATCH (same import pattern as `orders.py:1164`).

## Frontend (pages/components + what they show; restrained light UI)

Design constraint: neutral/monochrome + single accent, colour only for semantic meaning. No emojis, no multi-colour decorations.

**`frontend/src/pages/inventory/InventoryPage.tsx`**

- In the serialized stock ledger tab, add a `QUARANTINED` status chip: small, solid red-600 background, white text "Quarantined". Matches the existing chip pattern used for SOLD/AVAILABLE.
- Per-row action: "Mark Quarantine" button, visible only to `STORE_MANAGER+` roles and only when `status` is `AVAILABLE` or `DAMAGED`. Hidden (not disabled) for SOLD/TRANSFERRED units.
- Modal (inline, no new page): plain white card, gray-100 background.
  - Dropdown "Reason" (required): DEFECTIVE / SCRATCHED / CUSTOMER_RETURN_DAMAGED / RECEIVED_DAMAGED / QC_FAILED_WORKSHOP / OTHER.
  - Optional "Vendor" selector: pulls from the existing vendors list API (pre-filters to vendors with active relationships).
  - "Notes" textarea, max 200 chars, character counter.
  - Primary CTA: "Mark Quarantine + Print Label" (calls PATCH then POST label). Secondary: "Mark Only" (calls PATCH only; unit appears unlabeled in queue).
  - On success: row status chip flips to red "Quarantined"; toast success.

**New "Quarantine Queue" tab on `InventoryPage.tsx`**

- Tab label: "Quarantine" with a red dot badge showing count of `label_printed=false` units.
- Table columns: Product Name, Brand, Stock ID, Reason, Quarantined Date, Labeled?, RTV Vendor.
- "Labeled?" column: green "Yes" chip or red "No" chip with a "Print" button (calls POST label).
- "RTV Vendor" column: vendor name if `rtv_vendor_id` is set, else gray "—" + "Create RTV" button that navigates to vendor returns form pre-filled with `stock_id` and `vendor_id=null` (manager selects vendor on that form).
- Empty state: plain gray text "No quarantined units". No illustrations.

**`frontend/src/components/inventory/SerialNumberTracker.tsx`** (if exists)

- In the serial detail modal, add "Mark Quarantine" action button (same flow). When status is already QUARANTINED, show quarantine metadata: reason, date, quarantined-by-name.

## RBAC + flags (roles; feature-flag if POS/money)

**No feature flag** — this feature touches no POS payment path, no pricing, no order creation.

| Action | Roles |
|---|---|
| Mark quarantine (`PATCH /stock/{id}/quarantine`) | STORE_MANAGER, AREA_MANAGER, ADMIN, SUPERADMIN |
| Print label (`POST /labels/quarantine/{id}`) | STORE_MANAGER, AREA_MANAGER, ADMIN, SUPERADMIN |
| Lift quarantine (`PATCH /stock/{id}/lift-quarantine`) | STORE_MANAGER, AREA_MANAGER, ADMIN, SUPERADMIN |
| View quarantine queue (`GET /stock/quarantined`) | STORE_MANAGER (own store), AREA_MANAGER, ADMIN, ACCOUNTANT (read-only), SUPERADMIN |
| Link RTV to quarantined units (via VendorReturnCreate `stock_ids`) | ADMIN, AREA_MANAGER, STORE_MANAGER, ACCOUNTANT |
| SALES_CASHIER, SALES_STAFF, CASHIER, OPTOMETRIST, WORKSHOP_STAFF, CATALOG_MANAGER | No access to any quarantine action |

Store-scoping: STORE_MANAGER sees only their store's quarantine queue. AREA_MANAGER/ADMIN/SUPERADMIN can pass `store_id` param.

RBAC policy rows must be added to `backend/api/services/rbac_policy.py` for the three new endpoints. The `rbac_enforcement` middleware (already live) enforces at request time as a second layer.

## Acceptance tests (INTENT-LEVEL — what the test session asserts; a hollow shell must FAIL these)

1. **QUARANTINED blocks POS**: a unit marked QUARANTINED does not appear in a POS product search or cart (assert `GET /api/v1/inventory/stock` with `status=AVAILABLE` excludes it; a POS cart-add for its product shows available qty decremented by 1).

2. **QUARANTINED blocks transfers**: `_apply_ship_stock_move` with a transfer line whose product's only available unit is QUARANTINED must transfer zero units for that line and return a logged warning. A direct attempt to ship a QUARANTINED `stock_id` explicitly returns 400.

3. **Audit trail is complete and immutable**: marking quarantine, printing the label, and lifting quarantine each produce a distinct hash-chained `audit_logs` row with `entity_type=STOCK_UNIT`, correct `before_state`/`after_state`, and the right `action` code. `GET /api/v1/audit/verify` passes on the chain after all three writes.

4. **Label flip is atomic**: calling `POST /labels/quarantine/{stock_id}` on a unit that is NOT QUARANTINED returns 400 and does NOT flip `quarantine_label_printed`. Calling it on a valid QUARANTINED unit flips the flag to `true` and returns a label payload with `label_type=QUARANTINED`, `header` containing "QUARANTINED", and a `barcode_value` matching the unit's existing barcode.

5. **Luxury brand line appears on label**: a QUARANTINED label for a Cartier or Gucci product includes `luxury_brand_line: "BRAND AUTHORIZATION REQUIRED"`. A label for a standard brand does not include that field.

6. **Quarantine queue shows unlabeled count correctly**: after marking 3 units quarantined and printing labels for 2, `GET /stock/quarantined?label_printed=false` returns exactly 1 unit; the response summary shows `unlabeled_count: 1`.

7. **Lift quarantine is fully audited and reversible**: a manager lifts quarantine with a reason; the unit's status returns to AVAILABLE and appears in POS search again; the audit trail has a `QUARANTINE_LIFTED` row carrying the `lift_reason`. Attempting to lift a unit that is not QUARANTINED returns 409 `"not_quarantined"`.

8. **Store-scope isolation**: a STORE_MANAGER for store A calling `GET /stock/quarantined?store_id=B` receives an empty list (not store B's data). An AREA_MANAGER over both A and B sees both stores' quarantined units.

9. **RTV linkage backfills correctly**: creating a `VendorReturnCreate` with `stock_ids=["STO-XXXXXXXX"]` sets `rtv_vendor_id` on that `stock_units` doc and writes a `QUARANTINE_LINKED_RTV` audit row. A stock unit not in QUARANTINED status is silently skipped (not updated).

10. **Event bus fires on quarantine**: the `stock.quarantined` event is dispatched when a unit is quarantined. If the event bus is unavailable (no Redis), the quarantine PATCH still succeeds (fail-soft contract honoured).

11. **RBAC enforcement**: a SALES_CASHIER attempting `PATCH /stock/{id}/quarantine` receives 403. An ACCOUNTANT attempting the same receives 403. An ACCOUNTANT calling `GET /stock/quarantined` receives 200. A STORE_MANAGER for store A cannot lift a quarantine on a stock unit from store B (404/403).

12. **Period-lock does not block quarantine on current-period stock**: quarantine is a physical control, not a financial write; `check_period_locked` should be called but only raises for explicitly locked periods (same fail-soft contract as `finance.py:462`). Confirm the call is present for audit completeness but does not gate normal current-period operations.

## Effort (dev-days) + risk

**Effort: 2.5 dev-days**

| Work item | Days |
|---|---|
| Backend: quarantine + lift-quarantine endpoints + transfers guard | 0.75 |
| Backend: labels quarantine endpoint + luxury brand logic | 0.5 |
| Backend: vendor_returns schema extension + stock_ids backfill | 0.25 |
| Backend: audit rows + event bus dispatch + RBAC policy rows | 0.25 |
| Frontend: status chip + Mark Quarantine modal + Quarantine Queue tab | 0.5 |
| Tests (12 intent-level acceptance scenarios) | 0.25 |

**Risk: LOW**

- POS safety: zero risk. The POS catalog filter (`status=AVAILABLE`) already excludes any non-AVAILABLE unit. No POS code changes.
- Data: purely additive fields on `stock_units`. Existing docs without quarantine fields continue to work; all new fields are optional.
- Transfers: the hardening in `_apply_ship_stock_move` is tightening a silent skip into an explicit guard. This is behavior-improving, not behavior-changing for the normal path.
- Label printer dependency: QZ Tray falls back to 204 (same as today for workshop labels) if unconfigured. The quarantine mark itself succeeds; the queue flags unlabeled units. Self-healing.
- No accounting impact: quarantine creates no journal entry, no COGS change, no Tally entry. Financial exposure is zero.
- E3 readiness: the two-write pattern (stock_repo.update + AuditRepository.create) used here is exactly what E3's `record_event` will wrap when Wave-3 lands. No rework needed — E3 rewires the callers without changing their observable behavior.

## Definition of done

- `PATCH /stock/{id}/quarantine` returns 200 with updated doc; unit is absent from all `status=AVAILABLE` queries.
- `PATCH /stock/{id}/lift-quarantine` returns 200; unit re-appears as AVAILABLE.
- `GET /stock/quarantined` returns store-scoped list with correct `unlabeled_count`.
- `POST /labels/quarantine/{id}` returns a label payload including `header`, `barcode_value`, `quarantine_reason`, `quarantine_at`; `quarantine_label_printed` is `true` on the stock_unit after the call.
- Luxury brand label line present for Cartier/Gucci/etc., absent for standard brands.
- `VendorReturnCreate` accepts `stock_ids` and backfills `rtv_vendor_id` on matched QUARANTINED units.
- All 12 acceptance tests pass.
- `tsc --noEmit` exits 0 on the frontend.
- `rbac_policy.py` has entries for all three new endpoints; the RBAC coverage regression test passes.
- No emoji in any Python file; no dark-mode colour tokens in any new React component.